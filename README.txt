miniOrange API Authentication 

Moodle SSO solution by miniOrange allows your users to login to multiple applications via their Moodle credentials.
This way, you can achieve Single Sign-On into your applications where the users will need to authenticate themselves via their 
Moodle credentials only once and they can access all the configured applications.